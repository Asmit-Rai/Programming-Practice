#include<iostream>
using namespace std;
int main()
{
    string s1;
    string s2;
    cout<<"enter the string one"<<endl;
    cin>>s1;
    cout<<"enter the string two"<<endl;
    cin>>s2;
    cout<<"the size of the given strings are"<<s1.size()<<endl;
    cout<<"the size of the given strings are"<<s2.size()<<endl;

       //s1.size() is the function used to find the size of the string.
    
}